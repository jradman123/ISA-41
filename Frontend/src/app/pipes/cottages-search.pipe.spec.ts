import { CottagesSearchPipe } from './cottages-search.pipe';

describe('CottagesSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CottagesSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
