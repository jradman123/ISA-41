import { AdventureSearchPipe } from './adventure-search.pipe';

describe('AdventureSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AdventureSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
