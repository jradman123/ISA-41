import { ShipSearchPipe } from './ship-search.pipe';

describe('ShipSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ShipSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
